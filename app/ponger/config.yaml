service:
  port: 8080
  # If the following are uncommented, then the server will operate over TLS
  tlsCertificate: /app/certs/server.crt
  tlsPrivateKey: /app/certs/server.key

metrics:
  port: 2112
  endpoint: "/metrics"
